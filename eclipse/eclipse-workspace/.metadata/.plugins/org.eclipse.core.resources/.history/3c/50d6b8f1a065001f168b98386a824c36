package Bank_Front;

import Bank_View.MainPage;

public class FrontController {
	public static void main(String[] args) {

		int pageNum = 0; // 메인페이지
		boolean run = true;

		while (run) {
			if (pageNum == 0) {
				MainPage mainPage = new MainPage;
				Request request = mainPage.view();
			} else if (pageNum == 1) {
				// 계좌 생성
			} else if (pageNum == 2) {
				// 계좌 목록
			} else if (pageNum == 3) {
				// 입금
			} else if (pageNum == 4) {
				// 출금
			} else if (pageNum == 5) {
				// 프로그램 종료
				run = false;
				System.out.println("프로그램이 종료되었습니다.");

			}
		}
	}
}
